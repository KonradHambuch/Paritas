import type { Dict } from '../en';
import {
  meta, brand, nav, hero, services, team, sale, acquisition, method,
  contact, footer, notFound,
} from './content';
import { tools } from './tools';

export const hu: Dict = {
  meta, brand, nav, hero, services, team, sale, acquisition, method,
  contact, footer, notFound, tools,
};
