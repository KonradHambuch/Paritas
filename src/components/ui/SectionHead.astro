---
interface Props {
  eyebrow: string;
  heading: string;
  lede?: string;
}
const { eyebrow, heading, lede } = Astro.props;
---
<div class="eyebrow">{eyebrow}</div>
<h2>{heading}</h2>
{lede && <p set:html={lede} />}
